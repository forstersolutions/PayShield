FROM node:22-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci --omit=dev --ignore-scripts

COPY services/core ./services/core

ENV NODE_ENV=production
ENV PAYSHIELD_CORE_PORT=8080

EXPOSE 8080

CMD ["node", "services/core/server.mjs"]
